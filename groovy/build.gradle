apply plugin:'groovy'

dependencies {
  compile localGroovy()
  compile project(":validation.lambda.core")
}

apply plugin: 'codenarc'

codenarc {
  configFile = file("${projectDir}/codenarc.xml")
  reportFormat = 'html'
  ignoreFailures = true
  toolVersion = '1.0'
}
