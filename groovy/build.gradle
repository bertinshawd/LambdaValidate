apply plugin:'groovy'

dependencies {
  compile localGroovy()
  compile project(":lambdavalidation.core")
}

apply plugin: 'codenarc'

codenarc {
  configFile = file("${projectDir}/codenarc.xml")
  reportFormat = 'html'
  ignoreFailures = true
  toolVersion = '1.0'
}
